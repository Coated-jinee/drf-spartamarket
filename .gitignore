# 가상 환경
venv/
.venv/
__pycache__/
*.pyc
*.pyo

# OS 관련 파일
.DS_Store
*.swp

# 기타
*.egg-info
dist/
build/

# Django 관련
*.sqlite3       # SQLite 데이터베이스 파일 (개발 환경에서만 사용하는 경우)
local_settings.py # 로컬 설정 파일 (민감한 정보 포함 가능)
secret_key.txt  # Django secret key 파일 (절대 공유 금지)
media/          # 사용자 업로드 파일 (용량이 클 수 있음)
static/         # collectstatic 명령어로 생성되는 정적 파일 (배포 시 관리)

# IDE 관련 (선택적)
.vscode/        # VS Code 관련 파일
